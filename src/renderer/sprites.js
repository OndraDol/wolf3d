/**
 * Billboard sprite rendering — objekty a nepřátelé.
 * Vždy otočení čelem ke kameře, kreslení z procedurální geometrie.
 * Bude implementováno ve Fázi 4.
 */

/**
 * Nakresli viditelné sprity (seřazené od nejdál po nejblíž).
 * @param {Screen} screen
 * @param {Camera} camera
 * @param {Array} entities
 * @param {Float64Array} depthBuffer - z-buffer ze stěn pro clipping
 */
export function drawSprites(screen, camera, entities, depthBuffer) {
    // TODO: implementovat
}
