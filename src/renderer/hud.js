/**
 * HUD overlay — zdraví, munice, score, tvář hráče.
 * Vše kresleno Canvas 2D primitivy (žádné obrázky).
 * Bude implementováno ve Fázi 6.
 */

/**
 * Nakresli HUD přes 3D scénu.
 * @param {CanvasRenderingContext2D} ctx
 * @param {object} gameState
 */
export function drawHUD(ctx, gameState) {
    // TODO: implementovat
}
