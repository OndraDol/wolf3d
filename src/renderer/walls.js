/**
 * Vykreslování stěn z výsledků raycasteru.
 * Každý sloupec: výška z perpendicular distance, barva/textura z tile typu.
 * Bude implementováno ve Fázi 1.
 */

import { Screen } from './screen.js';

/**
 * Nakresli všechny sloupce stěn z pole RayHit výsledků.
 * @param {Screen} screen
 * @param {RayHit[]} rays
 */
export function drawWalls(screen, rays) {
    // TODO: implementovat
}
